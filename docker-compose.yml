version: '3'

volumes:
  app-db:

services:
  item-db:
    image: mongo:3
    volumes:
      - app-db:/data/db

  item-app:
    image: aprianfirlanda/item-app:v1
    ports:
      - "80:8080"
    depends_on:
      - item-db
    restart: unless-stopped
